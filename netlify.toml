[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/api/cloud-sync"
  to = "/.netlify/functions/cloud-sync"
  status = 200
  force = true
